{
  "name": "montreal-plus-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173 --bind 0.0.0.0",
    "build": "rm -rf dist && mkdir dist && cp index.html styles.css app.js manifest.webmanifest sw.js dist/",
    "check": "node --check app.js"
  }
}
